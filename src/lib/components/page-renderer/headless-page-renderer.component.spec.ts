import { describe, expect, it } from 'vitest';
import { HeadlessPageRendererComponent } from './headless-page-renderer.component';

describe('HeadlessPageRendererComponent', () => {
  it('exposes a standalone component contract', () => expect(HeadlessPageRendererComponent).toBeTruthy());
});
